#!/usr/bin/make -f
#export DH_VERBOSE=1
#
# Uncomment to ignore all test failures (but the tests will run anyway)
#export DH_RUBY_IGNORE_TESTS=all
#
# Uncomment to ignore some test failures (but the tests will run anyway).
# Valid values:
#export DH_RUBY_IGNORE_TESTS=ruby1.9.1 ruby2.0 require-rubygems
#
# If you need to specify the .gemspec (eg there is more than one)
#export DH_RUBY_GEMSPEC=gem.gemspec

%:
	dh $@ --buildsystem=ruby --with ruby

override_dh_auto_install:
	dh_auto_install
	mv debian/tuns/usr/lib/ruby/vendor_ruby/pNet-DNS/lib/Net debian/tuns/usr/lib/ruby/vendor_ruby/Net
	rm -rf debian/tuns/usr/lib/ruby/vendor_ruby/pNet-DNS
	cp tuns-client tuns-server tuns-netsetup debian/tuns/usr/sbin/
