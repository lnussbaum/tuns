# Defaults for tuns initscript
# sourced by /etc/init.d/tuns
# installed at /etc/default/tuns by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Run TUNS?
RUN="no"

# Options that are passed to the Daemon.
# See tuns-server -h
DAEMON_OPTS=""


